{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["ELECTRON_RENDERER_PORT", "VITE_WS_URL", "VITE_DEV_SERVER_URL", "WS_SERVER_PORT"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "dist-electron/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "smoke-test": {
      "dependsOn": ["build"],
      "cache": false,
      "outputs": []
    }
  }
}
